{
  "name": "jwordenasphalt",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && rsync -av --exclude='dist' --exclude='node_modules' --exclude='.git' . dist/ && html-minifier --input-dir dist --output-dir dist --collapse-whitespace --remove-comments --minify-css true --minify-js true --file-ext html && cleancss -o dist/styles.css dist/styles.css"
  },
  "devDependencies": {
    "clean-css-cli": "^5.6.3",
    "html-minifier": "^4.0.0"
  },
  "dependencies": {
    "nodemailer": "^8.0.3"
  }
}
